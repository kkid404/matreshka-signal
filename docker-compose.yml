version: "3.8"

services:
  scanner:
    build: .
    container_name: matryoshka-scanner
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    environment:
      - PYTHONUNBUFFERED=1
